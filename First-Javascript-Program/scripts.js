var profit = parseInt(prompt("Enter profit here."));
var revenue = parseInt(prompt("Enter revenue here."));

var Revenue = new Array(10);
for(var i=0;i<i.length;i++){
    revenue+500;
    profit + (profit * 1.15);
  }